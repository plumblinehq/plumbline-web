/**
 * Placeholder shell. The real application — the router, the layout and the
 * pages that read the server API — replaces this in the next commits.
 */
export function App() {
  return (
    <main className="mx-auto max-w-3xl px-6 py-16">
      <h1 className="text-3xl font-semibold tracking-tight text-slate-900">Plumbline</h1>
      <p className="mt-2 text-slate-600">The Stellar anchor conformance directory.</p>
    </main>
  );
}
